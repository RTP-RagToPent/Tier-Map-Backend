if [ -d "supabase/functions" ] && [ "$(ls -A supabase/functions/)" ]; then
  deno lint supabase/functions/
  deno fmt --check supabase/functions/
else
  echo "No functions found in supabase/functions/, skipping lint"
fi
